{
    "tasks": {
        "test": "deno test",
        "fmt": "deno fmt",
        "lint": "deno lint",
        "check": "deno check src/**/*.ts tests/**/*.ts bench/**/*.ts",
        "cli": "deno run --allow-read src/cli.ts",
        "compile": "deno compile --allow-read --output bin/specforge src/cli.ts",
        "bench": "deno bench bench/",
        "bench:compare": "deno run --allow-read bench/compare.ts"
    },
    "fmt": {
        "indentWidth": 4,
        "lineWidth": 100,
        "semiColons": true
    },
    "lint": {
        "rules": {
            "tags": ["recommended"]
        }
    }
}
